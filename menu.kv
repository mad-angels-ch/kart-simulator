#:import navigation_screen_manager navigation_screen_manager
#:import layouts layouts
#:import action_bar action_bar
#:import webbrowser webbrowser
#:import mainMenu1 mainMenu1
#:import playingMode_menu playingMode_menu
#:import create_or_join create_or_join
#:import join_game_menu join_game_menu
#:import createGame_menu createGame_menu
#:import rules rules
#:import credits credits
#:import log_in log_in
#:import kart_settings kart_settings
#:import settings settings
#:import music_settings music_settings
#:import controls controls
# Création de l'interface de tous les screens


<MyScreenManager>:
    Screen:
        name:"MainMenu"
        MainMenu1:
    Screen:
        name: "CreateSPGame"
        BoxLayoutWithActionBar:
            title: "Create/Join A Game"
            CreateGame:
    Screen:
        name: "CreateMPGame"
        BoxLayoutWithActionBar:
            title: "Create/Join A Game"
            CreateGame:
                FloatLayout:
                    TextInput:
                        id:gameName
                        font_size: 12
                        pos_hint: {"x":.499,"y":.91}
                        size_hint: .25,.045

                    Label:
                        font_size:16
                        pos_hint: {"x":.25,"y":.88}
                        size_hint: .25,.1
                        text: "Name of the game:"

    Screen:
        name:"CreateOrJoin"
        BoxLayoutWithActionBar:
            title: "Playing Mode"
            CreateOrJoin:

    Screen:
        name: "Playingmode"
        BoxLayoutWithActionBar:
            title: "Main Menu"
            PlayingMode:
    Screen:
        Widget:
            canvas:
                Color:
                    rgba: 0, 0, 0, 1
                Rectangle:
            BoxLayout:
                Image:
                    source: "client/Images/karts/Red_kart.png"
    Screen:
        name:"SettingsMenu"
        BoxLayoutWithActionBar:
            title: "Settings Menu"
            UserSettings:
    Screen:
        name:"Music_Settings"
        BoxLayoutWithActionBar:
            title:"Music Settings"
            MusicSettings:
    Screen:
        name:"Kart_Settings"
        BoxLayoutWithActionBar:
            title: "Kart settings"
            KartSettings:
    Screen:
        name: "LogIn"
        BoxLayoutWithActionBar:
            title: "Log In"
            LogIn:
    Screen:
        name: "Multiplayer"
        BoxLayoutWithActionBar:
            title: "Multiplayer"
            GameConnection:

    Screen:
        name: "Rules"
        BoxLayoutWithActionBar:
            title: "Rules of the game"
            Rules:
    Screen:
        name: "Controls"
        BoxLayoutWithActionBar:
            title: "Controls of the game"
            Controls:
    Screen:
        name: "UpdateWorlds"
        BoxLayoutWithActionBar:
            title: "Update The Worlds"
            UpdateWorlds:    
    Screen:
        name: "EG1"
        PasswordScreen:
            nbr:1

    Screen:
        name: "EG2"
        PasswordScreen:
            nbr:2
    Screen:
        name: "Credits"
        BoxLayoutWithActionBar:
            title: "Credits"
            Credits:

